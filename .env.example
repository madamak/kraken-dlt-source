# Kraken Futures credentials (base64 secret)
# Note: Use double underscore (__) for dlt configuration
KRAKEN_FUTURES__API_KEY=changeme
KRAKEN_FUTURES__API_SECRET=changeme

# Kraken Spot credentials (for future use)
# KRAKEN_SPOT__API_KEY=changeme
# KRAKEN_SPOT__API_SECRET=changeme

# Optional: override DLT destination (defaults to DuckDB)
# DLT_DESTINATION=duckdb
